<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team.ljm.secw.mapper.TopicCommentMapper">
    <delete id="deleteComment" parameterType="Integer">
        delete from t_topic_comment where id=#{id}
    </delete>
    <insert id="insertComment" parameterType="TopicComment">
        insert into t_topic_comment(topic_id,account,content,released_at) values(#{topicId},#{account},#{content},#{releasedAt})
    </insert>

</mapper>